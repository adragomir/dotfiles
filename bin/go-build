#!/bin/bash

PACKAGE=adobe/emulate_players
export GOPATH="${PWD}:${GOPATH}"
SRC="$PWD/src"
FULL="$SRC/$PACKAGE"

FULL_BASE=$(dirname "${FULL}")

if [ ! -d "${FULL_BASE}" ]; then
	mkdir -p "${FULL_BASE}"
fi

if [ ! -h "${FULL}" ]; then
	ln -s ../../../ "${FULL}"
fi

go $@ "${PACKAGE}"

