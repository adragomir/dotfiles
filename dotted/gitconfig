[user]
	name = Andrei Dragomir
	email = adragomir@gmail.com

[color]
	diff = auto
	status = auto
	branch = auto

[alias]
	st = status
	st2 = ls-files --exclude-per-directory=.gitignore \
				--exclude-from=.git/info/exclude \
				-t -o -u -s -m -d
	dc   = diff --stat --name-status --cached
	cat   = -p cat-file -p
	ci = commit
	co = checkout
	di = diff
	w = !git branch -a && git log --branches --oneline --graph --decorate --simplify-by-decoration --since='2 months ago'
	log1 = log --pretty=oneline --abbrev-commit --decorate
	lg = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr)%Creset' --abbrev-commit --date=relative
	file-blame = !git ls-files | xargs -n1 -i{} git log -1 --pretty=format:\"%ci {}\" -- {}

#[diff] 
#  external = /Users/adragomi/bin/araxisgitdiff
#  renames = true

[mergetool "araxismergetool"] 
  cmd = /Users/adragomi/bin/araxisgitmerge "$PWD/$REMOTE" "$PWD/$BASE" "$PWD/$LOCAL" "$PWD/$MERGED"
  trustExitCode = false

[mergetool]
  keepBackup = false

[merge] 
  tool = araxismergetool

[core]
	legacyheaders = false
	excludesfile = /Users/adragomi/.gitignore

[repack]
	usedeltabaseoffset = true

[gui]
	fontdiff = -family Courier -size 13 -weight normal -slant roman -underline 0 -overstrike 0

[github]
	user = adragomir
	token = 631abd318631e5d9ef2f932a811a8d7a
