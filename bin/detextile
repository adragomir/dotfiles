#!/usr/bin/env python

import sys
from detextile import detextile

if __name__ == "__main__":
    if len(sys.argv) > 1:
        inputFile = open(sys.argv[1], "r")
    else:
        inputFile = sys.stdin

    textile_ = ''.join(detextile.detextile(inputFile.read()))
    print textile_

